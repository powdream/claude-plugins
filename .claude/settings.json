{
  "permissions": {
    "allow": [
      "Skill(add-plugin)",
      "Skill(add-skill)",
      "Skill(commit)",
      "Bash(tree:*)",
      "Bash(shellcheck:*)",
      "Bash(echo:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if [[ \"$file_path\" == *.md ]]; then deno fmt \"$file_path\"; fi; }"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if [[ \"$file_path\" == *.sh ]]; then shellcheck \"$file_path\"; fi; }"
          }
        ]
      }
    ]
  }
}
