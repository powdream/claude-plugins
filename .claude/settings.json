{
  "permissions": {
    "allow": [
      "Skill(add-plugin)",
      "Skill(add-skill)",
      "Skill(commit)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if echo \"$file_path\" | grep -qE '\\.md$'; then deno fmt \"$file_path\"; fi; }"
          }
        ]
      }
    ]
  }
}
